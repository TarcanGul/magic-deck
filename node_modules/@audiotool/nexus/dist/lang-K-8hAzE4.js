const s = (t) => {
  throw t instanceof Error ? t : new Error(t);
};
function c(t, r) {
  if (!t)
    throw new Error(r ?? "assertion failed");
}
const i = (t, r) => new Promise((e) => {
  if ((r == null ? void 0 : r.aborted) ?? !1) {
    e();
    return;
  }
  const o = setTimeout(e, t);
  r == null || r.addEventListener(
    "abort",
    () => {
      clearTimeout(o), e();
    },
    { once: !0 }
  );
}), w = (t, r, e) => {
  const o = new AbortController(), n = Promise.withResolvers(), a = async () => {
    for (; ; ) {
      if (o.signal.aborted)
        return n.resolve();
      await t(o.signal), await i(r, o.signal);
    }
  };
  return (e == null ? void 0 : e.immediateTrigger) ?? !0 ? a() : i(r, o.signal).then(a), {
    terminate: async () => {
      o.abort(), await n.promise;
    }
  };
};
export {
  c as a,
  w as b,
  i as s,
  s as t
};
