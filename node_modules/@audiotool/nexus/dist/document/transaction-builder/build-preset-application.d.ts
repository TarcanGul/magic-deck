import { Modification } from '../../gen/audiotool/document/v1/document_service_pb';
import { NexusEntity } from '../entity';
import { EntityQuery } from '../query/entity';
import { PreparedPreset } from './prepare-preset';

/** @internal Builds a list of modifications that apply a preset to a main entity.
 *
 * `presetName` is the backend identifier of the applied preset (format
 * `presets/<uuid>`). It is stamped onto `mainEntity.presetName` so the document
 * records which preset is currently loaded. Pass an empty string to clear it.
 */
export declare const buildModificationsForPresetApplication: (entities: EntityQuery, presetInfo: PreparedPreset, mainEntity: NexusEntity, presetName: string) => Modification[];
