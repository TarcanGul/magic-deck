import { EntityConstructorType, EntityTypeKey } from '../../entity-utils';
import { Defaults } from './default-type';

/**
 * A map of entity type keys to their default values.
 * Used to look up defaults when creating entities or resetting fields.
 */
export declare const entityDefaultsMap: Record<EntityTypeKey, Defaults<EntityConstructorType>>;
