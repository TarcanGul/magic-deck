import { Modification } from '../../gen/audiotool/document/v1/document_service_pb';
import { PrimitiveField, PrimitiveType } from '../fields';
import { SchemaLocation } from '../schema/schema-location';

/** Builds a modification resetting the value of a `PrimitiveField` to the value
 * defined by the defaults map with all the predetermined defaults.
 * Returns undefined iif the field is already at its default value or it's of type NexusLocation.
 */
export declare const buildModificationForFieldReset: <P extends PrimitiveType>(field: PrimitiveField<P, "mut">) => Modification | undefined;
/**
 * Gets the default value for a schema location from the entityDefaultsMap.
 */
export declare const getDefaultForSchemaLocation: (schemaLocation: SchemaLocation) => DefaultsPrimitive | Error;
type DefaultsPrimitive = string | number | bigint | boolean | Uint8Array;
export {};
